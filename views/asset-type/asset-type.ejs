<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/head %> 
</head>
<body>
<% include ../partials/menu %>
<div class="container bg-3">

  <div class="row">
    <div class="col-sm">
      <h1><%= page %></h1>
    </div>
  </div>

  <%# Asset Types %>
  <div class="row">
    <div class="col-sm">
      <% if (assetTypes.length > 0) {%>
        <div class="card">
          <ul class="list-group list-group-flush">
          <% assetTypes.forEach(assetType => { %>
              <li class="list-group-item">
                <a href="/asset-type/asset-type/<%= assetType.id %>"><%= assetType.name %></a>
                <small class="form-text text-muted"><%= assetType.description %></small>
              </li>
          <% }) %>
          </ul>
        </div>
      <% } else { %>
        <p>No Asset Types</p>
      <% } %>
    </div>
  </div>

  <%# Add %>
  <div class="row my-3">
    <div class="col-sm">
      <a class="btn btn-info mt-2 mb-3" href="/asset-type/add/">Add</a>
    </div>
  </div>

</div>
<% include ../partials/script %>
</body>
</html>
